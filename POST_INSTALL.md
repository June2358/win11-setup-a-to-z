# ⚡ Power User Tips & Customization

윈도우 사용성을 극대화하는 개인 설정, 최적화, 그리고 유용한 팁 모음입니다.

---

## 🎨 1. UI 및 개인화 (Personalization)

### 1.1. 바탕화면 정리
깔끔한 바탕화면 유지를 위한 설정입니다.

- **바탕화면 우클릭** -> `보기`
    - [x] **큰 아이콘** (시원시원한 보기)
    - [x] **아이콘 자동 정렬** (흐트러짐 방지)

#### 기본 바탕화면 아이콘 표시
윈도우 설치 직후 숨겨져 있는 기본 시스템 아이콘을 바탕화면에 표시합니다.

- **설정** -> `개인 설정` -> `테마` -> `바탕 화면 아이콘 설정`
    - [x] **컴퓨터 (이 PC)**
    - [x] **사용자 파일**
    - [x] **제어판**
    - [x] **휴지통**
    - [ ] **네트워크** (필요한 경우)

> **사용자 파일**은 `C:\Users\사용자명`에 해당하는 사용자 프로필 폴더로 이동하는 아이콘입니다.

### 1.2. 작업 표시줄 다이어트
불필요한 아이콘을 제거하여 작업 공간을 확보합니다.

- **설정** -> `개인 설정` -> `작업 표시줄`
    - [ ] 검색 (아이콘만 표시 or 숨기기)
    - [ ] 작업 보기 (끔)
    - [ ] 위젯 (끔)

> Windows 11 버전에 따라 표시되는 항목은 조금씩 달라질 수 있습니다. 사용하지 않는 항목만 선택적으로 숨기면 됩니다.

### 1.3. 파일 탐색기 최적화
탐색기를 열었을 때 기본 `홈(Home)` 화면 대신 `내 PC`가 뜨도록 변경하고, 불필요한 최근 기록을 표시하지 않도록 설정합니다.

- **파일 탐색기** -> `...` (더보기) -> `옵션`
    - **파일 탐색기 열기**: [내 PC]로 변경
    - **개인 정보 보호**:
        - [ ] 최근에 사용한 파일 표시 (해제)
        - [ ] 자주 사용하는 폴더 표시 (해제)

#### 파일 확장자 표시
파일의 실제 형식을 쉽게 확인할 수 있도록 확장자를 항상 표시합니다.

- **파일 탐색기** -> `보기` -> `표시`
    - [x] **파일 이름 확장명**

예:
```text
문서.hwp
사진.jpg
압축파일.zip
프로그램.exe
```

파일 확장자를 표시해두면 파일 관리가 편해지고, `문서.pdf.exe`처럼 파일 형식을 오인하기 쉬운 실행 파일도 구분하기 쉽습니다.

### 1.4. 시작 메뉴 클린하게
- **설정** -> `개인 설정` -> `시작`
    - [ ] 최근에 추가된 앱 표시 (해제)
    - [ ] 자주 사용하는 앱 표시 (필요에 따라 해제)
    - [ ] 시작, 점프 목록 및 파일 탐색기에서 최근에 연 항목 표시 (해제)
    - [ ] 팁, 바로가기, 새 앱 등에 대한 추천 표시 (표시되는 경우 해제)

> Windows 11 업데이트에 따라 시작 메뉴의 항목 이름이나 구성은 조금씩 달라질 수 있습니다.

### 1.5. 검색 엔진 변경 (Bing 탈출)
엣지(Edge)나 크롬 주소창에서 검색 시 Bing 대신 Google이 나오도록 설정합니다.

- **Edge 브라우저**: `설정` -> `개인 정보, 검색 및 서비스` -> `검색 및 연결된 경험` -> `주소 표시줄 및 검색` -> 검색 엔진을 **[Google]** 로 변경.
- **Chrome 브라우저**: `설정` -> `검색엔진` -> 주소 표시줄에서 사용되는 검색 엔진 -> **[Google]** 확인.

### 1.6. ChatGPT 바탕화면 바로가기 만들기
자주 사용하는 Chrome 프로필에서 ChatGPT를 바로 열 수 있도록 바탕화면 바로가기를 만듭니다.

1. 사용할 **Google 계정의 Chrome 프로필**을 선택합니다.
2. **[ChatGPT](https://chatgpt.com/)**에 접속하여 원하는 계정으로 로그인합니다.
3. Chrome 우측 상단의 `⋮` 버튼을 클릭합니다.
4. `캐스트, 저장, 공유` -> **바로가기 만들기...**를 선택합니다.
5. 이름을 **ChatGPT**로 지정하고 `만들기`를 누릅니다.

이후 바탕화면의 **ChatGPT** 아이콘을 통해 해당 Chrome 프로필 환경에서 빠르게 접속할 수 있습니다.

> 여러 Google 계정을 사용하는 경우에는 원하는 계정의 **Chrome 프로필에서 ChatGPT에 로그인한 뒤** 바로가기를 만드는 것이 좋습니다.

> 별도 창에서 앱처럼 사용하고 싶다면 `캐스트, 저장, 공유` -> **페이지를 앱으로 설치...**를 사용할 수도 있습니다.

---

## ⌨️ 2. 입력 장치 최적화 (Input Devices)

게이밍 및 빠른 작업을 위한 설정입니다.

### 2.1. 키보드 반응 속도
키를 누르고 있을 때 반복 입력되는 딜레이와 속도를 조정합니다.

- **제어판** -> `키보드`
    - **재입력 시간**: [짧게] (최대 우측으로)
    - **반복 속도**: [빠름] (최대 우측으로)

> 입력 감각에 영향을 주는 설정이므로 취향에 따라 조절하면 됩니다.

### 2.2. 마우스 가속 끄기
Windows의 마우스 가속 기능을 끄면 마우스 이동 속도에 따른 포인터 이동량 변화를 줄일 수 있습니다.

- **제어판** -> `마우스` -> `포인터 옵션` 탭
    - [ ] **포인터 정확도 향상** (체크 해제)

FPS 게임 등에서 일정한 마우스 감각을 선호한다면 끄고 사용하는 경우가 많습니다.

### 2.3. 한컴 입력기 정리
한컴오피스를 설치하면 **한컴 입력기**가 추가될 수 있습니다.

게임이나 일부 프로그램에서 한/영 전환, 전각/반각 입력 등과 관련된 문제가 발생한다면 아래 두 방법 중 하나를 사용합니다.

#### 방법 1. 한컴 입력기 삭제 (권장)
한컴 입력기를 사용하지 않는다면 Windows에서 입력기 자체를 제거할 수 있습니다.

- **설정** -> `시간 및 언어` -> `언어 및 지역`
- **한국어** 오른쪽의 `...` -> `언어 옵션`
- `키보드` 항목에서 **한컴 입력기** -> `...` -> `제거`

이후 **Microsoft 입력기**만 사용하면 입력기가 실수로 전환되는 것을 방지할 수 있습니다.

#### 방법 2. Microsoft 입력기로 변경
한컴 입력기를 삭제하지 않고 필요할 때만 Microsoft 입력기로 전환할 수도 있습니다.

1. `Win` + `Space`를 누르거나 작업 표시줄의 입력기 아이콘을 클릭합니다.
2. **Microsoft 입력기**를 선택합니다.

> 게임이나 프로그램에서 한글 입력 문제가 발생한다면 현재 **한컴 입력기 / Microsoft 입력기 중 무엇이 선택되어 있는지** 먼저 확인하세요.

---

## 🚀 3. 시스템 관리 및 광고 차단 (System Management & AdBlock)

쾌적한 환경을 위해 광고를 차단하고, Windows의 불편한 설정을 관리하는 도구 모음입니다.

### 3.1. 웹 브라우저 광고 차단
Chrome과 Edge 모두 **uBlock Origin Lite**를 기본으로 사용합니다.

#### Google Chrome
Chrome은 Manifest V3 기반 확장 프로그램을 사용하므로 **uBlock Origin Lite**를 설치합니다.

- **[uBlock Origin Lite 다운로드](https://chromewebstore.google.com/detail/ublock-origin-lite/ddkjiahejlhfcafbddmgiahcphecmpfh)**
- **특징**:
    - Manifest V3 기반
    - 광고 및 트래커 차단
    - uBlock Origin 개발자 Raymond Hill(gorhill)이 개발

#### Microsoft Edge
Edge 역시 Manifest V2에서 Manifest V3로 전환 중이므로 장기적으로는 **uBlock Origin Lite**를 사용하는 편이 관리하기 쉽습니다.

- **[uBlock Origin Lite 다운로드](https://microsoftedge.microsoft.com/addons/detail/ublock-origin-lite/cimighlppcgcoapaliogpjjdehbnofhn)**

> 기존 uBlock Origin은 Firefox에서 계속 사용할 수 있으며, Chromium 계열 브라우저에서는 Manifest V3 전환에 따라 상황이 달라지고 있습니다.

### 3.2. Microsoft PC Manager (선택)
Microsoft에서 제공하는 Windows PC 관리 도구입니다.

필수 프로그램은 아니지만 여러 관리 기능을 하나의 화면에서 사용하고 싶을 때 편리합니다.

- **[Microsoft PC Manager 다운로드](https://pcmanager.microsoft.com/)**

- **주요 기능**:
    - 임시 파일 및 저장 공간 관리
    - 시스템 상태 확인
    - 시작 프로그램 관리
    - 지원되는 앱의 팝업 관리

> 메모리를 강제로 비우는 것 자체가 항상 PC 성능 향상을 의미하지는 않습니다. PC가 느리다면 시작 프로그램과 실제 CPU/RAM/디스크 사용량을 먼저 확인하는 것이 좋습니다.

### 3.3. 시스템 전체 광고 차단 (심화/유료)
브라우저 확장 프로그램보다 넓은 범위에서 네트워크 트래픽을 필터링하려면 **AdGuard for Windows** 같은 설치형 프로그램을 사용할 수 있습니다.

- **[AdGuard for Windows](https://adguard.com/)**: 유료 소프트웨어
- **특징**:
    - 브라우저뿐 아니라 지원되는 Windows 앱의 트래픽 필터링
    - 광고 및 추적 차단
    - 앱별 필터링 설정 지원

- **설정 팁**:
    1. `설정` -> `네트워크`에서 **HTTPS 필터링** 상태를 확인합니다.
    2. 필요한 경우 특정 앱이나 사이트를 필터링 대상에서 제외할 수 있습니다.

> HTTPS 필터링은 암호화된 웹 트래픽을 검사하기 위해 AdGuard 인증서를 사용하는 기능이므로 동작 방식을 이해하고 사용하는 것이 좋습니다.

> AdGuard와 브라우저용 콘텐츠 차단기를 동시에 사용할 수도 있지만 필터가 중복되면 사이트 오작동이나 문제 원인 파악이 어려울 수 있습니다. 특별한 이유가 없다면 하나를 주력으로 사용하는 편이 관리하기 쉽습니다.

### 3.4. Windows 초기 설정·트윅 도구 참고
인터넷에서 출처가 불분명한 `.reg`, `.bat`, `.exe` 파일을 무작정 실행하기보다는 널리 사용되는 오픈소스 프로젝트의 설명과 코드를 참고하는 편이 좋습니다.

#### Chris Titus Tech's Windows Utility (WinUtil)
- **GitHub**: https://github.com/ChrisTitusTech/winutil
- 프로그램 설치, Windows 설정, 업데이트 관리 등을 한곳에 모은 종합 도구입니다.
- 다양한 Windows 초기 설정을 확인하기에 좋습니다.

#### Winhance
- **GitHub**: https://github.com/memstechtips/Winhance
- 개인정보 보호, 탐색기, UI, 전원, 게임 등의 Windows 설정을 GUI에서 변경할 수 있습니다.
- 옵션을 하나씩 살펴보며 적용하기 편합니다.

#### Win11Debloat
- **GitHub**: https://github.com/Raphire/Win11Debloat
- Windows 11의 불필요한 앱, 광고, 추천 및 일부 기본 설정을 정리하는 데 특화된 프로젝트입니다.
- 명령줄과 선택적 설정 적용을 지원합니다.

> **주의:** 유명한 최적화 도구라고 해서 모든 옵션을 한꺼번에 적용할 필요는 없습니다.

> 특히 **Windows Update, Defender, SmartScreen, 페이지 파일, Windows Search, 메모리 관리, 각종 서비스**는 기능을 정확히 확인한 뒤 변경하세요.

---

## 📂 4. 참고: 한글(HWP) 리소스 경로 정보 (Advanced)

한글 프로그램이 구동될 때 참조하는 주요 이미지 파일의 위치입니다.
시스템 튜닝이나 UI 분석을 원하는 경우 참고할 수 있습니다.

- **대상 경로**:
  ```
  C:\Program Files (x86)\HNC\Office 2022\HOffice120\Bin\UxXml\Hancom2016\Common\Image\ko-kr\
  ```
  *(※ 주의: 설치된 한글 버전에 따라 `Office 2022`, `Hancom2016` 등의 폴더명은 다를 수 있습니다. 해당 경로가 없다면 상위 폴더에서 유사한 경로를 찾아보세요.)*

- **주요 파일**: **`ci.png`**
  - 해당 파일이 프로그램 실행 시 표시되는 **로고 이미지**입니다.
  - *참고: 이 파일이 존재하지 않거나 로딩되지 않으면 실행 시 로고가 표시되지 않을 수 있습니다.*
